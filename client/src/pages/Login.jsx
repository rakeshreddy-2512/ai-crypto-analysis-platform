import { useState } from 'react'; import { api } from '../api/client';
export default function Login(){const [form,setForm]=useState({email:'demo@crypto.ai',password:'Password123!'});const submit=async(e)=>{e.preventDefault();const {data}=await api.post('/auth/login',form);localStorage.setItem('token',data.token);window.location='/profile';};return <form onSubmit={submit} className='glass p-6 m-4 max-w-md'><h2>Login</h2><input className='w-full p-2 my-2 text-black' placeholder='email' value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className='w-full p-2 my-2 text-black' type='password' value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className='bg-cyan-500 px-4 py-2 rounded'>Login</button></form>}
